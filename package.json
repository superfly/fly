{
  "name": "fly-root",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build": "lerna run --parallel build",
    "rebuild": "lerna run --parallel rebuild",
    "clean": "lerna run --parallel clean"
  },
  "devDependencies": {
    "lerna": "^3.1.1",
    "typescript": "3.0.1"
  }
}