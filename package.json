{
  "name": "fly-root",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run --parallel build",
    "rebuild": "lerna run --parallel rebuild",
    "bundle": "lerna run bundle",
    "clean": "lerna run --parallel clean",
    "clean:dependencies": "rm -rf node_modules packages/*/node_modules",
    "dev": "lerna run --parallel dev",
    "test": "yarn test:unit && yarn test:e2e",
    "test:unit": "jest",
    "test:e2e": "yarn jest --config=./tests/e2e/jest.config.js"
  },
  "devDependencies": {
    "@types/jest": "^23.3.1",
    "jest": "^23.5.0",
    "lerna": "^3.1.1",
    "ts-jest": "^23.1.4",
    "typescript": "3.0.1"
  }
}